# Система управления товарами (Магазин обуви)

Веб-приложение на Django 5.2 для управления товарами с ролевой системой доступа.

## Установка и запуск

1. Активируйте виртуальное окружение:
   ```bash
   venv\Scripts\activate
   ```

2. Установите зависимости (уже установлены в venv)

3. Создайте и примените миграции:
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

4. Создайте суперпользователя для админ-панели:
   ```bash
   python manage.py createsuperuser
   ```

5. Соберите статические файлы:
   ```bash
   python manage.py collectstatic --noinput
   ```

6. Запустите сервер:
   ```bash
   python manage.py runserver
   ```

7. Добавьте данные через админ-панель: http://127.0.0.1:8000/admin/

## Варианты заданий

В папке `документы/` находятся три варианта заданий для занятий. Каждый вариант имитирует экзамен: переделка базового шаблона путём изменения моделей и форм.

- **Вариант 1:** Магазин обуви (базовый)
- **Вариант 2:** Книжный магазин
- **Вариант 3:** Магазин электроники

Подробнее: `документы/README.md`

## Роли пользователей

### Гость
- Просмотр списка товаров без фильтров, сортировки, поиска

### Авторизированный клиент
- Просмотр списка товаров без фильтров, сортировки, поиска

### Менеджер
- Просмотр товаров с фильтрацией, сортировкой, поиском
- Просмотр заказов

### Администратор
- Все возможности менеджера
- CRUD операции с товарами
- CRUD операции с заказами

## Функциональность

### Товары
- **Просмотр:** Список товаров с пагинацией
- **Поиск:** По названию, описанию, категории, производителю, поставщику
- **Фильтрация:** По поставщику
- **Сортировка:** По названию, количеству (возр./убыв.)
- **CRUD:** Создание, редактирование, удаление (только администратор)

### Заказы
- **Просмотр:** Список заказов с пагинацией
- **CRUD:** Создание, редактирование, удаление (только администратор)

### Визуальные особенности
- Подсветка товаров с скидкой >15% (зеленый фон)
- Подсветка товаров не в наличии (голубой фон)
- Перечеркнутая цена с отображением скидки

## Структура проекта

```
django_de/
├── manage.py
├── shoe_store/          # Основные настройки проекта
├── products/            # Приложение товаров
├── orders/              # Приложение заказов
├── accounts/            # Приложение аутентификации
├── templates/           # Шаблоны
├── static/              # Статические файлы
├── media/               # Медиа файлы
├── документы/           # Варианты заданий для занятий
└── db.sqlite3           # База данных
```

## Модели данных

### Товары (Product)
- Название, категория, описание
- Производитель, поставщик
- Цена, единица измерения, количество
- Скидка, изображение

### Заказы (Order)
- Артикул, статус, пункт выдачи
- Дата заказа, дата доставки
- Клиент, позиции заказа

## Технологии

- **Backend:** Django 5.2
- **Frontend:** Bootstrap 5, HTML/CSS
- **База данных:** SQLite
- **Изображения:** Pillow
